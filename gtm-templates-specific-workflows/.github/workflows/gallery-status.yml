name: Gallery Status

on:
  schedule:
    - cron: '0 3 * * *'
  push:
    branches:
      - main
      - master
    paths:
      - README.md

jobs:
  gallery-status:
    uses: stape-io/template-changes-shared-workflows/.github/workflows/gallery-status.yml@main
    secrets: inherit
